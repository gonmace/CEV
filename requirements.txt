Django>=5.1
whitenoise>=6.8
django-tailwind>=4.0
django-crispy-forms>=2.3
crispy-tailwind>=1.0
pillow>=11.0
python-decouple>=3.8
psycopg2-binary>=2.9
gunicorn>=23.0
django-axes>=7.0
django-csp>=3.8
django-allauth>=65.0
django-redis>=5.4
redis>=5.2
PyJWT>=2.8

# pliego_licitacion / proyectos
beautifulsoup4>=4.12
python-docx>=1.0
markdown>=3.8
nh3>=0.2  # sanitiza el HTML que produce `markdown` antes de mark_safe/|safe (ver core/sanitize.py)
pygments>=2.17  # usado por markdown.extensions.codehilite (dependencia implícita, no declarada antes)
requests>=2.31.0

# servicios
pdfplumber>=0.11
pymupdf>=1.24
easyocr>=1.7
img2table>=1.3

# PyTorch CPU-only (evita instalar librerías CUDA en VPS sin GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
